# Demo video script (&lt;3 min) — Axion-true

Record on the **live Axion VM**. Optional ≠ skip — best WOW lever.

## Shot list

| Time | Visual | Voice |
|---|---|---|
| 0:00–0:15 | Grafana cost panel (real scrape) | "Agentic AI wastes most of every dollar. NeuroSwarm-Arm on Arm Neoverse cuts that — here's live $/token." |
| 0:15–0:45 | Terminal: `curl` chat → cascade `tier_used` | "Three-tier CPU cascade. KleidiAI llama.cpp. No GPU." |
| 0:45–1:20 | `docker compose ps` IMAGE column | "Proof: `nexus-arm/llama-kleidiai:server` — not stock llama.cpp." |
| 1:20–2:00 | Performix Instruction Mix / Code Hotspots | "Arm Performix — recipes named in the rules. Instruction Mix shows SVE2/I8MM utilization." |
| 2:00–2:30 | Router / tools Top-K | "Semantic MCP router: top-K schemas, not the whole flood." |
| 2:30–2:50 | One slide: Option A | "Auto-detect NUMA/CXL/MTE. Axion single-NUMA degrades cleanly; multi-socket unlocks split/CXL." |
| 2:50–3:00 | Logo + repo URL | "Clone, `docs/setup.md`, reproduce." |

## Must show

- Real device (Axion) — rules still reward live device footage  
- KleidiAI image name  
- Non-empty Grafana / metrics  
- ≥1 Performix artifact  

## Upload

YouTube / Vimeo / Youku. Link on Devpost. Keep raw file in `demo/` if size allows (git LFS optional).
