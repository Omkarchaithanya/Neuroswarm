# RTG Future Improvements

1. Train L3 PPO from Mem0 `(agent_id, workflow_id)` profiles with online GEPA.
2. Per-token SSE hooks when llama.cpp exposes logprobs / think delimiters.
3. Wire `thinking_token_budget` through LlamaCppBackend HTTP extras.
4. Performix MCP recipe → energy joules FinOps panel.
5. Cross-VM swarm budget via Redis when HAOE multi-node lands.
6. SGLang backend + shared RTG hook path.
