# Gateway container runtime (Dockerfile.arm64). Mirrors pyproject [project.optional-dependencies].gateway.
# Install: pip install -r requirements-gateway.txt && pip install ./packages/okf && pip install --no-deps -e .

# Core API
fastapi>=0.115
uvicorn[standard]>=0.34
starlette>=0.47
pydantic>=2.11
pydantic-settings>=2.10

# Async / HTTP
anyio>=4.10
httpx>=0.28
aiofiles>=24.1
tenacity>=9.1

# Config / serialization
pyyaml>=6.0
python-dotenv>=1.1
orjson>=3.11

# Router + runtime numerics (neuroswarm_arm/runtime/router/*)
numpy>=2.3
# Pillar 2: BGE-small via FastEmbed (ONNX Runtime) + TurboVec ANN
fastembed>=0.8.0
onnxruntime>=1.17.0
turbovec>=0.8.0

# KV / MAKS default compression (zstd) + OKF slot affinity hashing
zstandard>=0.23
lz4>=4.3
psutil>=6.1
blake3>=1.0
mcp>=1.12
# MCP template stdio servers (web-search / github / slack / s3) spawned by NSA_MCP_EXECUTE
fastmcp==3.4.4
slack_sdk>=3.33
aiohttp>=3.10
boto3>=1.35

# Observability (NSA_DIPA_OTEL defaults on)
opentelemetry-api>=1.36
opentelemetry-sdk>=1.36
opentelemetry-exporter-otlp>=1.36
prometheus-client>=0.22

# Utilities (OKF CLI + logging)
rich>=14
typer>=0.16
loguru>=0.7
